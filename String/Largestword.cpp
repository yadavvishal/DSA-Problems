#include<bits/stdc++.h>
using namespace std;
int check(string a,string b)
{
     if(a==b)
     {
        
     }
}
int main()
{
    vector<string> dict
        = { "ale", "apple", "monkey", "plea" };
    string str = "abpcplea";
    sort(dict.begin(),dict.end());
    for(int i=0;i<dict.size();i++)
    {
         check(dict[i],str)
    }
     
}